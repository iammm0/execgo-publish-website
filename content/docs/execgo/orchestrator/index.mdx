---
title: "Orchestrator：Adopting ExecGo from an Upper Layer"
description: "ExecGo runs your submitted TaskGraph asynchronously using a DAG scheduler."
---

ExecGo runs your submitted `TaskGraph` asynchronously using a DAG scheduler.
So your orchestration layer (Agent framework / workflow engine) mainly needs to:

1. Translate your workflow DAG into an ExecGo `TaskGraph`
2. Correctly interpret ExecGo result semantics (`success/failed/skipped`)
3. Implement asynchronous polling (and, if needed, idempotent submit)

## Document entry points

- [Mapping：DAG -> TaskGraph](/docs/execgo/orchestrator/mapping-dag-to-taskgraph)
- [Failure semantics：failed vs skipped](/docs/execgo/orchestrator/failure-semantics)
- [Polling & idempotency：stable submit & read results](/docs/execgo/orchestrator/polling-and-idempotency)

## Read these References first

- [`Task DSL` (task model & validation)](/docs/execgo/reference/task-dsl)
- [`API endpoints` (POST /tasks and GET /tasks/\{id\})](/docs/execgo/reference/api)

---

Reminder: `POST /tasks` returns `202 Accepted` immediately. You must poll `GET /tasks/\{id\}` to obtain final states.
